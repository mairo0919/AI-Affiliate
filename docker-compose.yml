services:
  research-agent:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_VERSION: "24"
        PNPM_VERSION: "11.17.0"
    env_file:
      - .env
    restart: unless-stopped
