# Initial Operations (ASSISTED)

Daily soft target 1–2（品質優先）。

Flow:

公開情報 → Research → Topic → Strategy → Claims → Blogger生成 → Quality Gate → 人間承認 → Blogger Draft → X生成 → X Export → 人間投稿 → Analytics → Evaluation → Learning → 次回Strategy

Skip when: ネタ不足 / Claim不足 / Quality不足 / Policy / Budget / pause。

CTA priority without Affiliate API: FANZA通常商品URL。
